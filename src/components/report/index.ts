// The session report page. Two mount points only — the history session list and
// the content area — and both must pass the DECISION A gate in `useSessionData`
// before rendering anything from here.

export { SessionReportView } from "./SessionReportView";
export { SessionEntryList } from "./SessionEntryList";
